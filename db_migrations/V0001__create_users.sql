CREATE TABLE IF NOT EXISTS users (id SERIAL PRIMARY KEY, login VARCHAR(64) UNIQUE NOT NULL, display_name VARCHAR(128) NOT NULL, password_hash VARCHAR(256) NOT NULL, role VARCHAR(32) NOT NULL DEFAULT 'user', status VARCHAR(16) NOT NULL DEFAULT 'active', bio VARCHAR(512) DEFAULT '', created_at TIMESTAMP NOT NULL DEFAULT NOW(), last_seen TIMESTAMP)